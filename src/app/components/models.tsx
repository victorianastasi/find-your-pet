import { Timestamp } from "firebase/firestore";

export type Item = {
    name: string;
    description: string;
    age: string;
    type: string;
    image: string;
    district: string;
    location: string;
    date: string;
  };